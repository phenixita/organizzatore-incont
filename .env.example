# Azure Storage Configuration
# Copy this file to .env.local and fill in your values

# Azure Storage Account name (created by Bicep deployment)
VITE_AZURE_STORAGE_ACCOUNT=your-storage-account-name

# Azure Storage Container name (default: app-data)
VITE_AZURE_STORAGE_CONTAINER=app-data

# Azure Storage SAS token (generate using deploy.sh instructions)
# The token should start with ?sv=...
VITE_AZURE_STORAGE_SAS=?sv=2021-06-08&ss=b&srt=sco&sp=racwdl&se=...

# Note: For local development without Azure, the app will automatically
# fall back to using localStorage. Just don't set the variables above.
